pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo building ${BUILD_TAG}'
            }
        }
        stage('Test') {
            steps {
                sh 'echo testing'
            }
        }
        stage('Deploy') {
            steps {
                sh ''
            }
        }
    }
}